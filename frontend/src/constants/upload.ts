/** Maximum file size in MB for standard uploads (course materials, report cards, etc.) */
export const MAX_FILE_SIZE_MB = 30;

/** Maximum number of files per upload session */
export const MAX_FILES_PER_SESSION = 10;
