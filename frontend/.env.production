VITE_API_URL=
VITE_SHOW_ERROR_DETAILS=false
VITE_STRIPE_PUBLISHABLE_KEY=
# Set by CI at build time (e.g. `VITE_BUILD_DATE=$(date +%F) npm run build`).
# Consumed by CompliancePage to render "Last updated" (#3681). Optional —
# the page falls back to a hardcoded date when unset.
VITE_BUILD_DATE=
