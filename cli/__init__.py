"""Standalone CLI tools for ClassBridge.

These are operator/admin scripts run from the repo root, not part of the
runtime FastAPI service. Each module is invokable directly:

    python cli/extract_ceg.py --pdf <path> --grade <n> --subject <code> --out <path>
"""
